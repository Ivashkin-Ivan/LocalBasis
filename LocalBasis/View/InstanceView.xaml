<Window x:Class="LocalBasis.View.InstanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LocalBasis.View"
             mc:Ignorable="d" 
             Height="400" Width="600" ResizeMode="NoResize">
    <Grid>
        <Grid/>
        <TextBlock Text=" Для получения координат элемента нажмите Track:"
                   Background="LightGoldenrodYellow" Margin="10,10,100,256"/>
        <Button x:Name="BTN_Save" 
                VerticalAlignment="Top" 
                HorizontalAlignment="Right"
                Content="Track" 
                Margin="0,8,25,0" 
                Width="65"/>
        <TextBlock x:Name="Global"
                    Margin="10,33,25,142"  
                    Padding="10,10,10,10"
                    Background="AliceBlue"/>
        <ScrollViewer Margin="10,154,10,10">
            <TextBlock x:Name="Locals" 
                       Margin="0,0,0,0"  
                       Padding="10,10,10,10"
                       Background="LightYellow"/>
        </ScrollViewer>
    </Grid>
</Window>
